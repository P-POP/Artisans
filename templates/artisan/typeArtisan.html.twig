{% extends "base.html.twig" %}

{% block title %}Types{% endblock %}

{% block body %}

{{dump(allArtisansFromType)}}

   {{dump(type)}}

       {% for artisan in allArtisansFromType %}

 
            {% if artisan.id == type.id %}
            
                {{artisan.name}}
            
            {% endif %}
        

        
       {% endfor %}

{% endblock %}